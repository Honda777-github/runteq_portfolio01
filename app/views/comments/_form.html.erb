<div class="row mb-3" id="comment-form">
  <div class="col-lg-8 offset-lg-2">
  <%= form_with(model: [ @shop, Comment.new ]) do |form| %>
    <div>
      <%= form.label :content, "コメント" %>
      <%= form.text_area :content, required: true %>
    </div>
    <div>
      <%= form.submit "投稿する" %>
    </div>
  <% end %>
  </div>
</div>

